# Prefix for media file URLs (profile pictures, post images).
# Leave empty to use relative paths — works with Vite proxy (dev) and nginx proxy (prod).
# Only set to a full origin (e.g. http://localhost:3000) if you are NOT using a reverse proxy.
VITE_API_URL=
